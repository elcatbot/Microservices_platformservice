
docker build -t platform_service .
docker build -t command_service .
docker-compose up -d --build

docker login
docker tag <your-image-name> <username>/<repository>:<tag>
docker push <username>/<repository>:<tag>
docker search <username>/<repository>


sudo snap install microk8s --classic
# Add your user to the microk8s group so you don't need 'sudo' every time
sudo usermod -a -G microk8s $USER
sudo chown -f -R $USER ~/.kube
# Apply the group changes
newgrp microk8s

# DNS: Allows services to talk to each other by name (like RabbitMQ).
# Hostpath-storage: Allows your SQL database to save data to your Ubuntu disk.
microk8s enable dns hostpath-storage dashboard 

kubectl version    

kubectl apply -f platforms-depl.yaml

kubectl get deployments

kubectl get pods       

kubectl delete deployment platforms-depl

kubectl apply -f platforms-np-srv.yaml

kubectl get services

kubectl get namespace

kubectl get pods --namespace=ingress-nginex

kubectl get services --namespace=ingress-nginex


kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v0.48.1/deploy/static/provider/cloud/deploy.yaml

kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.8.2/deploy/static/provider/cloud/deploy.yaml

kubectl patch svc ingress-nginx-controller -n ingress-nginx -p '{\"spec\": {\"type\": \"LoadBalancer\", \"externalIPs\":[\"192.168.1.3\"]}}'

kubectl create secret generic mmsql --from-literal=SA_PASSWORD="pa55w0rd!"


microk8s kubectl get ingress